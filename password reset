bcrypt.hash('Quincy123!', 12).then(hash => {
  pool.query(\"UPDATE users SET password_hash = \$1 WHERE username = 'eagyeibo' RETURNING username\", [hash])
    .then(r => { console.log('Password reset for:', r.rows[0].username); pool.end(); });
});
"